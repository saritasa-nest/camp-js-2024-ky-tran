/** Representing different URL type parameters for updating. */
export enum UpdateUrlParamsType {

	/** Type for pagination. */
	Paginator = 'paginator',

	/** Type for sorting. */
	Sort = 'sort',

	/** Type for filtering. */
	Filter = 'filter',

	/** Type for searching. */
	Search = 'search',
}
