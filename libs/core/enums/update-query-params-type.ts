/** Representing different URL type parameters for updating. */
export enum UpdateQueryParamsType {

	/** Type for pagination. */
	Paginator = 'paginator',

	/** Type for sorting. */
	Sort = 'sort',

	/** Type for filtering. */
	Filter = 'filter',

	/** Type for searching. */
	Search = 'search',
}
